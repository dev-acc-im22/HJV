import type { Metadata } from "next";
import { Montserrat } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const montserrat = Montserrat({
  variable: "--font-montserrat",
  subsets: ["latin"],
  weight: ["300", "400", "500", "600", "700", "800"],
});

export const metadata: Metadata = {
  title: "Happy Jodi Vibes - Find Your Perfect Match",
  description: "India's Most Trusted Matrimony Service. Search by City, Profession & Community to find your perfect life partner.",
  keywords: ["Matrimony", "Marriage", "Happy Jodi Vibes", "Wedding", "Life Partner", "Matchmaking", "Jodi"],
  authors: [{ name: "Happy Jodi Vibes Team" }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${montserrat.variable} font-montserrat antialiased bg-background text-foreground`}
        suppressHydrationWarning
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
